---
# tasks file for mysql

- name: Install mysql packages
  apt:
  name: mysql
  state: present
  with_items:
    - mysql
    - python-mysqldb
    - mysql-client
    - mysql-server
    - MySQL-python

- name: Mot de passe root
  MySQL-python:
    name: root
    password: root
    priv: '*.*:ALL'
    state: present
